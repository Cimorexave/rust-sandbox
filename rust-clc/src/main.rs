fn main() {
    //outputting
    // println!("Hello, world!");

    println!(
        "this is the {0} index and {1} is the next {index} {2}. You could do a {lot}",
        0,
        1,
        2,
        index = "index",
        lot = "lot of things with this string formatting capablitiy of rust..."
    );
}
